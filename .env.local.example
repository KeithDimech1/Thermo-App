# Thermochronology Database Configuration
# Copy this file to .env.local and fill in your database URL

# =============================================================================
# DATABASE CONNECTION
# =============================================================================

# Neon (serverless PostgreSQL - recommended for production)
# Get your connection string from: https://console.neon.tech
DATABASE_URL=postgresql://neondb_owner:YOUR_PASSWORD@ep-xxx.c-3.us-east-1.aws.neon.tech/neondb?sslmode=require

# Direct connection URL (required for migrations)
DIRECT_URL=postgresql://neondb_owner:YOUR_PASSWORD@ep-xxx.c-3.us-east-1.aws.neon.tech/neondb

# =============================================================================
# EXAMPLE CONFIGURATIONS
# =============================================================================

# Local Development (macOS/Linux with PostgreSQL installed)
# DATABASE_URL=postgresql://localhost/thermo_db
# DIRECT_URL=postgresql://localhost/thermo_db

# Local Development (Docker)
# DATABASE_URL=postgresql://postgres:postgres@localhost:5432/thermo_db
# DIRECT_URL=postgresql://postgres:postgres@localhost:5432/thermo_db

# Supabase (use connection pooling URL for DATABASE_URL, direct URL for DIRECT_URL)
# DATABASE_URL=postgresql://postgres.xxx:[PASSWORD]@aws-0-us-east-1.pooler.supabase.com:6543/postgres
# DIRECT_URL=postgresql://postgres:[PASSWORD]@db.xxx.supabase.co:5432/postgres

# =============================================================================
# INSTRUCTIONS
# =============================================================================

# 1. Copy this file to .env.local:
#    cp .env.local.example .env.local

# 2. Fill in your Neon database connection details from console.neon.tech

# 3. Test the connection:
#    npm run db:test

# 4. Create the schema:
#    npm run db:schema

# 5. Import the thermochronology data:
#    npm run db:import-thermo

# 6. Verify the import:
#    npm run db:verify

# =============================================================================
# PRODUCTION DEPLOYMENT (VERCEL)
# =============================================================================

# Add these environment variables in Vercel dashboard:
# 1. Go to your project settings -> Environment Variables
# 2. Add DATABASE_URL with your Neon connection string
# 3. Add DIRECT_URL with your Neon direct connection string
# 4. Make sure both are available for Production, Preview, and Development

# =============================================================================
# NOTES
# =============================================================================

# - Never commit .env.local to git (it's in .gitignore)
# - SSL is required for Neon connections (automatically enabled)
# - DIRECT_URL is required for migrations (pooled connections don't support migrations)
# - For production, both URLs should point to the same database
